# Generated or vendored — Prettier must not own these.
node_modules
dist
build
.agents
.claude
# Agent working state for an in-flight plan: gitignored, never committed, and
# rewritten mid-task. Prettier failing the lint gate on it blocks a push for a
# file that is not part of the repo.
.superpowers
test-results
playwright-report
supabase/.temp
bun.lock

# Python virtualenvs (the RAGAS eval harness). Some wheels vendor browser JS.
.venv

# Python test cache (regenerated on every pytest run).
.pytest_cache
